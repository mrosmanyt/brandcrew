══════════════════════════════════════════════════════════════════
  Cinem AI Assistant — Personal Intelligent Cyber Assistant
  Installation Guide
══════════════════════════════════════════════════════════════════

HOW TO INSTALL
--------------
Just download CINEM-AI-ASSISTANT-Setup.exe and run it. That's it.

The installer automatically:
  1. Installs the full Cinem AI Assistant app
  2. Installs all dependencies for you (Node.js, Python,
     Playwright Chromium browser, Faster-Whisper voice engine)
     — a console window shows live progress, please let it finish
  3. Creates a Desktop shortcut named "Cinem AI Assistant"
  4. Creates a Start Menu entry
  5. Offers "Run Cinem AI Assistant" when installation completes

REQUIREMENTS
------------
  • Windows 10 / 11 (64-bit)
  • Internet connection during installation (for dependency downloads)
  • ~1 GB free disk space

FIRST RUN
---------
  1. Launch Cinem AI Assistant from the desktop shortcut.
  2. Fill the registration form (Name, Email, WhatsApp, Country).
  3. Wait for admin approval — Cinem AI Assistant unlocks automatically.

TROUBLESHOOTING
---------------
  • If voice or browser automation features are missing, re-run the
    dependency setup:  <install folder>\bootstrap\install-deps.ps1
    (right-click → Run with PowerShell)
  • SmartScreen warning? Click "More info" → "Run anyway"
    (the installer is unsigned).

Enjoy Cinem AI Assistant!
══════════════════════════════════════════════════════════════════
